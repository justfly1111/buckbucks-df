# $FreeBSD$
#
# .shrc - bourne shell startup file 
#
# This file will be used if the shell is invoked for interactive use and
# the environment variable ENV is set to this file.
#
# see also sh(1), environ(7).
#


# file permissions: rwxr-xr-x
#
# umask	022

# Uncomment this to enable the builtin vi(1) command line editor in sh(1),
# e.g. ESC to go into visual mode.
# set -o vi


# some useful aliases
alias h='fc -l'
alias j=jobs
alias m="$PAGER"
alias ll='ls -laFo'
alias l='ls -l'
alias g='egrep -i'
 
# # be paranoid
# alias cp='cp -ip'
# alias mv='mv -i'
# alias rm='rm -i'


# set prompt: ``username@hostname:directory $ '' 
PS1="\u@\h:\w \\$ "

# search path for cd(1)
# CDPATH=:$HOME

#XKB_DEFAULT_LAYOUT="us.kbd"; export XKB_DEFAULT_LAYOUT
#HIKARI_CONFIG=$HOME/.config/hikari/hikari.conf; export HIKARI_CONFIG
#XDG_RUNTIME_DIR=/var/run/user/1002; export XDG_RUNTIME_DIR
##QTDIR=/usr/local/lib/qt5; export QTDIR
#_JAVA_AWT_WM_NONREPARENTING=1; export _JAVA_AWT_WM_NONREPARENTING
#SDL_VIDEODRIVER=wayland; export SDL_VIDEODRIVER
#MOZ_ENABLE_WAYLAND=1; export MOZ_ENABLE_WAYLAND
#WAYLAND_DISPLAY=wayland-1; export WAYLAND_DISPLAY
#HIKARI_CONFIG=/home/doeboy1111/.config/hikari/hikari.conf; export HIKARI_CONFIG
#QT_QPA_PLATFORM=wayland-egl; export QT_QPA_PLATFORM
#WAYFIRE_CONFIG_FILE=/home/doeboy1111/.config/wayfire.ini; export WAYFIRE_CONFIG_FILE
#CLUTTER_BACKEND=wayland; export CLUTTER_BACKEND
#GDM_LANG=en_US.UTF-8; export GDM_LANG
#GNOME_SESSION_AUTOSTART_DIR=$HOME/.config/autostart; export GNOME_SESSION_AUTOSTART_DIR

PATH=/sbin:/bin:/usr/sbin:/usr/bin ;export PATH
source .aliases
source .envvar

